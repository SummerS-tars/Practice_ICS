/**
 * optimized linear_sum
 */
long linear_sum(int max)
{
    return (long)max * (max + 1) / 2;
}

/**
 * optimized square_sum
 */
long square_sum(int max)
{
    return (long) max * (max + 1) * (2 * max + 1) / 6 ;
}